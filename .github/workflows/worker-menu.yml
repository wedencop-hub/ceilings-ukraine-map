name: Worker Menu Bot

on:
  push:
    paths:
      - 'worker-menu/**'
      - '.github/workflows/worker-menu.yml'
  workflow_dispatch:
  schedule:
    - cron: '0 */5 * * *'

concurrency:
  group: worker-menu-bot
  cancel-in-progress: true

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 290
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r worker-menu/requirements.txt

      - name: Run bot
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        run: python worker-menu/bot.py
